# Building the EXE

The repository will use GitHub Actions to build the Windows installer automatically. The local PowerShell build helper is included for developers who have Inno Setup 6 installed.

The final installer name is:
`ECOM-ICS-Local-Agent-Setup.exe`
